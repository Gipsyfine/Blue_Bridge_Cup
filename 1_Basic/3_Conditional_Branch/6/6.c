//判断一个正整数是否是两位数（即大于等于10且小于等于99）。
//输入：一个正整数，不超过1000。
//输出：一行。若该正整数是两位数，输出1，否则输出0。
#include <stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    if ( 10<=n && n<=99) {//不能直接写10<=n<=99
        printf("1");
    } else {
        printf("0");
    }
    return 0;
}
